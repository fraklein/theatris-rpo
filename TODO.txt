
OSC API
========

* marks containers/methods that are already implemented

Sending
--------

*/heartbeat
/outputX
    /is_connected
    /slotX
        /state
        /is_initialized
        /is_playing
        /is_loop_active
        /is_pushing_other_slots
        /current_source
        /curent_fade_time
        /current_elapsed_time
                        # query the current position of the stream
                        ret, current = self.playbin.query_position(
                            Gst.Format.TIME)
                        if not ret:
                            print("ERROR: Could not query current position")


Receiving
---------

*/outputX
    */stop_all
    */slotX
        */play_by_number(number:int, restart_when_already_playing: bool)
        */stop
        */set_alpha
        */play_test
        */pause
        */cfg_set_full_alpha_at_start(On_Off:bool)
        */cfg_set_loop(On_Off:bool)
        /cfg_set_fade_time(seconds: float) # maybe two times: in and out?
        /cfg_set_push_other_slots(On_Off:bool)
        /seek(time:?)
        /play # play already initialized pipeline. This might be required for speed.
*/stop_all


TODO: Create shell script that starts the app
TODO: Create systemd unit file to start app at startup
TODO: Create a shared directory for the video files
TODO: Describe how to prepare the raspberry for headless "silent" booting

